# Tested SageAttention version. Newer releases may work but are not validated here.
sageattention==1.0.6

# Windows-only Triton package commonly needed for SageAttention on Windows.
triton-windows; platform_system == "Windows"
