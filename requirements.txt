# Easiest tested SageAttention install path.
# SageAttention 2.2.0 was also validated, but may require a source build from upstream tag v2.2.0.
sageattention==1.0.6

# Windows-only Triton package commonly needed for SageAttention on Windows.
triton-windows; platform_system == "Windows"
